import math
def circle_area(radius):

    return round(math.pi*radius**2,2)

def triangle_area(height,side_length):
    return round(0.5*height*side_length,2)

def rectangle_area(side_a, side_b):
    return round(side_a*side_b,2)